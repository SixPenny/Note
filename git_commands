git add 
git commit -m 'comments'
git rm <file>
git log
git reflog   -- show the operations you do
git reset --hard HEAD^   //^表示会退到上一个版本
git status
git checkout -- file   //丢弃掉工作区内的修改
git reset HEAD file   //文件回退到某个版本
git clone remote_addr  //从远程库克隆一份到本地

branchs command

git checkout -b branch_name //创建并检出branch_name分支,相当于
  git branch branch_name
  git checkout branch_name 
git branch     //查看所有的分支,当前分支有＊标示
git checkout master //切换到master分支
git merge branch_name //合并branch_name到当前分支上
git branch -d branch_name // 删除分支




